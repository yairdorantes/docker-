services:
  web:
    build: .
    ports:
      - "8000:8000"
    # volumes:
    #   - ./django_test:/app
    # depends_on:
    #   - db
    # environment:
    #   - DEBUG=False
    #   - DB_HOST=db
    #   - DB_NAME=yourdbname
    #   - DB_USER=yourdbuser
    # - DB_PASS=yourdbpassword
    restart: always
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
    restart: always
